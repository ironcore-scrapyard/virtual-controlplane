apiVersion: v1
kind: Service
metadata:
  name: {{ include "kine.fullname" . }}
  labels:
    {{- include "kine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kine.selectorLabels" . | nindent 4 }}
